# Network Security
bind 0.0.0.0
protected-mode no
port 6379

# Persistence Settings
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# Memory Management (must align with compose limits)
maxmemory 1073741824
maxmemory-policy volatile-lru
