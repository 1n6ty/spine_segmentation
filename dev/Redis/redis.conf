# Network Security
bind 0.0.0.0
protected-mode no
port 6379

# Persistence Settings for Production
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# Memory Management (Must align with your compose limits)
maxmemory 1073741824
maxmemory-policy volatile-lru